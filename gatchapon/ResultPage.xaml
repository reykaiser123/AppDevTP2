<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="gatchapon.ResultPage"
             Title="ResultPage"
             Background="#ADC179">
    <ScrollView>
        <Grid RowDefinitions="Auto,*,Auto,Auto,Auto" Padding="20">


            <Label x:Name="goldcoin"
                Grid.Row="0"
                   Text=""
                TextColor="White"
                FontSize="18"
                HorizontalOptions="Center"
                Margin="0,0,0,10"/>      
            <Label x:Name="PullCountLabel"
               Grid.Row="1"
               TextColor="White"
               FontSize="18"
               HorizontalOptions="Center"
               Margin="0,0,0,10"/>

            
            <CollectionView x:Name="ResultCollection"
                        Grid.Row="2"
                        HorizontalOptions="Center"
                        VerticalOptions="Center"
                        ItemsLayout="HorizontalGrid, 2">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame HasShadow="False"
                           BackgroundColor="Transparent"
                           Padding="5"
                           Margin="5"
                           HorizontalOptions="Center"
                           VerticalOptions="Center">
                            <Image Source="{Binding Image}"
                               HeightRequest="100"
                               WidthRequest="100"
                               Aspect="AspectFit"/>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            
            <Button Grid.Row="3"
                Text="Pull Again"
                Clicked="OnPullAgainClicked"
                BackgroundColor="#6B63FF"
                TextColor="White"
                CornerRadius="12"
                HeightRequest="50"
                HorizontalOptions="Center"
                WidthRequest="200"
                Margin="0,10,0,0"/>

            
            <Button Grid.Row="4"
                Text="Close"
                Clicked="OnCloseClicked"
                BackgroundColor="#9B84EE"
                TextColor="White"
                CornerRadius="12"
                HeightRequest="50"
                HorizontalOptions="Center"
                WidthRequest="200"
                Margin="0,10,0,0"/>
        </Grid>
    </ScrollView>
</ContentPage>